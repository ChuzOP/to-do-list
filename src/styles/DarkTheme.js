import { createTheme } from "@mui/material/styles";

const DarkTheme = createTheme({
  palette: {
    background: {
      default: "#FAF2F2",
    },
    text: {
      primary: "#080808",
      secondary: "#080808",
    },
    primary: {
      main: "#E8AA42",
    },
    secondary: {
      main: "#1F4690",
    },
  },
});

export default DarkTheme;
